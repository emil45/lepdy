---
phase: 14-puzzle-pool-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - data/chessPuzzles.ts
autonomous: true
requirements:
  - PGEN-01
  - PGEN-02

must_haves:
  truths:
    - "Movement puzzle pool contains 60+ puzzles with at least 10 per piece and 10 per tier"
    - "Capture puzzle pool contains 30+ puzzles with at least 5 per piece and 10 per tier"
    - "Every puzzle has a difficulty field of 1, 2, or 3"
    - "Every movement puzzle validTargets matches chess.js moves() output"
    - "Every capture puzzle has exactly one correct attacker and zero distractors that can reach target"
    - "New puzzle IDs follow {piece}-move-{tier}-{n} and capture-{piece}-{tier}-{n} convention"
    - "Existing puzzle IDs and data shapes are preserved (no breaking changes)"
  artifacts:
    - path: "data/chessPuzzles.ts"
      provides: "60+ movement puzzles and 30+ capture puzzles with 3 difficulty tiers"
      contains: "pawn-move-3-1"
      min_lines: 500
  key_links:
    - from: "data/chessPuzzles.ts"
      to: "scripts/validate-puzzles.ts"
      via: "validation script verifies all puzzle data"
      pattern: "npx tsx scripts/validate-puzzles.ts"
    - from: "data/chessPuzzles.ts"
      to: "data/chessPieces.ts"
      via: "import ChessPieceId type"
      pattern: "import.*ChessPieceId"
---

<objective>
Author 42 new movement puzzles and 22 new capture puzzles in data/chessPuzzles.ts, expanding the pool from 26 to 90 puzzles with full difficulty tier coverage.

Purpose: This is the core data expansion that enables PGEN-01 and PGEN-02 — random puzzle selection without rapid repetition. Every puzzle is validated by the script from Plan 01 before being committed.

Output: Expanded `data/chessPuzzles.ts` with 60 movement + 30 capture puzzles across 3 difficulty tiers
</objective>

<execution_context>
@/Users/emil/code/lepdy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emil/code/lepdy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-puzzle-pool-expansion/14-CONTEXT.md
@.planning/phases/14-puzzle-pool-expansion/14-RESEARCH.md
@.planning/phases/14-puzzle-pool-expansion/14-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From data/chessPuzzles.ts:
```typescript
export interface MovementPuzzle {
  id: string;                    // Format: '{piece}-move-{tier}-{n}' for new, keep existing IDs
  pieceId: ChessPieceId;         // 'king' | 'queen' | 'rook' | 'bishop' | 'knight' | 'pawn'
  fen: string;                   // Piece-placement only (8 ranks, no side-to-move suffix)
  pieceSquare: string;           // Algebraic notation, e.g. 'e4'
  validTargets: string[];        // All legal destination squares per chess.js
  difficulty: 1 | 2 | 3;        // 1=easy, 2=medium, 3=hard
}

export interface CapturePuzzle {
  id: string;                    // Format: 'capture-{piece}-{tier}-{n}' for new
  fen: string;                   // All pieces on board (target=lowercase, player=uppercase)
  targetSquare: string;          // Black piece to capture
  correctPieceSquare: string;    // White piece that can reach target
  correctPieceId: ChessPieceId;  // Piece type of correct attacker
  targetPieceId: ChessPieceId;   // Piece type of target
  distractorSquares: string[];   // White pieces that CANNOT reach target
  difficulty: 1 | 2 | 3;        // 1=easy(1 distractor), 2=medium(1-2), 3=hard(2-3)
}
```

From data/chessPieces.ts:
```typescript
export type ChessPieceId = 'king' | 'queen' | 'rook' | 'bishop' | 'knight' | 'pawn';

// FEN characters: K=king, Q=queen, R=rook, B=bishop, N=knight, P=pawn
// Lowercase for black pieces: k, q, r, b, n, p
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 42 new movement puzzles across 6 pieces and 3 tiers</name>
  <files>data/chessPuzzles.ts</files>
  <read_first>
    data/chessPuzzles.ts
    data/chessPieces.ts
    .planning/phases/14-puzzle-pool-expansion/14-RESEARCH.md
    scripts/validate-puzzles.ts
  </read_first>
  <action>
Add 42 new movement puzzles to the `movementPuzzles` array in `data/chessPuzzles.ts`. Keep all 18 existing puzzles unchanged. Add 7 new puzzles per piece type (rook, bishop, queen, knight, king, pawn) to reach 10 total per piece.

**ID convention:** `{piece}-move-{tier}-{n}` where tier is 1/2/3 and n is a sequential number. Examples: `rook-move-1-1`, `rook-move-2-1`, `rook-move-3-1`.

**Tier distribution per piece** (7 new puzzles each, counting existing difficulty tags):
- Check existing puzzles' difficulty values to determine how many of each tier already exist
- Target: at least 3 per tier per piece (3+3+4 or 4+3+3 across the 10 total)
- Fill gaps with new puzzles to balance tiers

**Difficulty tier rules (LOCKED from CONTEXT.md):**

Tier 1 (easy): Piece on center/open square (c3-f6 area), empty board, no blocking pieces.
- FEN has only the single piece (e.g., `'8/8/8/8/4R3/8/8/8'`)
- Pieces: any piece type

Tier 2 (medium): Piece on edge rank or file (rank 1/8, file a/h), OR slightly off-center. Empty board for simple pieces (rook, king, pawn). Add 1 white blocking pawn for complex pieces (bishop, knight, queen).
- Edge positions: restrict movement directions, reducing validTargets count
- Blocking pawn: use uppercase `P` only (same color, cannot be captured)
- FEN example with blocker: `'8/8/3P4/8/3R4/8/8/8'` (pawn on d6 blocks rook on d4)

Tier 3 (hard): Piece near edge or corner with 1-2 white pawn blockers on board.
- Child must understand that pieces (except knight) cannot jump through blockers
- Use `P` (white pawn) as blocker — visually distinct, children know pawns can't jump
- Knight tier 3: use edge/corner positions (fewer targets) — knights ignore blockers
- King tier 3: corner or near-edge with reduced escape squares

**Critical rules for position design:**
- Movement puzzle boards must NEVER contain opponent (lowercase) pieces — this prevents pawn capture diagonals from appearing
- All blocking pieces must be white (uppercase P) — same color as the puzzle piece
- Avoid pawns on rank 7 (promotion creates duplicate moves)
- Avoid placing pieces where dummy kings (a1/h1 for white, h8/a8 for black) would conflict — keep main puzzle piece and blockers away from corners when possible
- For king puzzles, place dummy black king in the OPPOSITE corner from the white king (at least 3+ squares away) to avoid adjacent-king restrictions

**Compute validTargets using chess.js** (do NOT hand-compute):
- For each puzzle, construct the FEN, add dummy kings, call `new Chess(fen + ' w - - 0 1').moves({verbose: true, square: pieceSquare})`, and use the resulting target squares
- Deduplicate with `new Set()` for pawn promotions
- Sort validTargets alphabetically for consistency

After adding all 42 puzzles, run `npx tsx scripts/validate-puzzles.ts` to verify all pass. Fix any failures before committing.
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && npx tsx scripts/validate-puzzles.ts 2>&1 | grep -E "(movement|errors|warnings|total)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - data/chessPuzzles.ts contains at least 60 entries in `movementPuzzles` array
    - data/chessPuzzles.ts contains `rook-move-1-1` (new rook tier 1 puzzle)
    - data/chessPuzzles.ts contains `pawn-move-3-1` (new pawn tier 3 puzzle)
    - data/chessPuzzles.ts contains `knight-move-3-1` (new knight tier 3 puzzle)
    - data/chessPuzzles.ts contains `bishop-move-2-1` (new bishop tier 2 puzzle)
    - All 18 existing movement puzzle IDs still present (rook-move-1, rook-move-2, ..., pawn-move-3)
    - `npx tsx scripts/validate-puzzles.ts` reports 0 errors for movement puzzles
    - No movement puzzle FEN contains lowercase letters (no opponent pieces)
  </acceptance_criteria>
  <done>60 movement puzzles (18 existing + 42 new) pass validation, with at least 10 per piece and at least 3 per tier per piece</done>
</task>

<task type="auto">
  <name>Task 2: Author 22 new capture puzzles across 6 pieces and 3 tiers</name>
  <files>data/chessPuzzles.ts</files>
  <read_first>
    data/chessPuzzles.ts
    scripts/validate-puzzles.ts
    .planning/phases/14-puzzle-pool-expansion/14-RESEARCH.md
  </read_first>
  <action>
Add 22 new capture puzzles to the `capturePuzzles` array in `data/chessPuzzles.ts`. Keep all 8 existing capture puzzles unchanged (including the fixed capture-rook-1 from Plan 01). Add enough puzzles per piece to reach 5 per piece as correctPieceId.

**Existing capture puzzle counts by correctPieceId:**
- rook: 2 (capture-rook-1, capture-rook-2) → need 3 more
- bishop: 1 (capture-bishop-1) → need 4 more
- knight: 2 (capture-knight-1, capture-knight-2) → need 3 more
- queen: 1 (capture-queen-1) → need 4 more
- king: 1 (capture-king-1) → need 4 more
- pawn: 1 (capture-pawn-1) → need 4 more
- Total new: 3+4+3+4+4+4 = 22

**ID convention:** `capture-{piece}-{tier}-{n}`. Examples: `capture-rook-2-1`, `capture-bishop-3-1`.

**Difficulty tier rules (LOCKED from CONTEXT.md):**

Tier 1 (easy): 1 distractor. Attacker is rook/queen/king — obvious straight-line or adjacent path.
- FEN has: 1 lowercase target piece, 1 uppercase correct attacker, 1 uppercase distractor
- Distractor CANNOT reach target (validate with chess.js)

Tier 2 (medium): 1-2 distractors. Attacker is bishop/knight — requires diagonal or L-shape vision.
- More visually complex board but still one correct answer
- Mix of target piece types (pawns, minor pieces, major pieces)

Tier 3 (hard): 2-3 distractors. Any piece type. Non-trivial geometry — multiple plausible-looking pieces but only one valid.
- Distractors should look like they might reach the target to a novice (e.g., a rook on same rank but blocked by another piece)
- Target piece types: mix pawns, knights, bishops, rooks, queens

**Target piece variety:** Use a mix of target piece types:
- At least 8 puzzles with pawn targets (lowercase `p`)
- At least 5 puzzles with minor piece targets (lowercase `n` or `b`)
- At least 5 puzzles with major piece targets (lowercase `r` or `q`)

**Critical validation rules:**
- The correct piece on `correctPieceSquare` MUST be able to reach `targetSquare` (verified by chess.js `moves({verbose:true, square})`)
- EVERY distractor on `distractorSquares[i]` MUST NOT be able to reach `targetSquare`
- All player pieces are uppercase (white), target piece is lowercase (black)
- Place dummy kings for chess.js validation: white K at a1/h1 (whichever free), black k at h8/a8 (whichever free)
- For king capture puzzles, the white king IS the correct piece — only add dummy black king
- Avoid placing distractors where they create pins against the dummy king

**Position design guidelines:**
- Vary target square locations across the board (don't cluster all targets in one area)
- Use different piece combinations for distractors across puzzles
- For tier 3, make distractors plausible-looking: e.g., rook on same file as target but blocked by another piece between them

After adding all 22 puzzles, run `npx tsx scripts/validate-puzzles.ts` to verify ALL puzzles pass (movement + capture). Fix any failures before committing.
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && npx tsx scripts/validate-puzzles.ts 2>&1 | grep -E "(capture|errors|warnings|total)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - data/chessPuzzles.ts contains at least 30 entries in `capturePuzzles` array
    - data/chessPuzzles.ts contains `capture-bishop-2-1` (new bishop tier 2 puzzle)
    - data/chessPuzzles.ts contains `capture-king-3-1` (new king tier 3 puzzle)
    - data/chessPuzzles.ts contains `capture-pawn-2-1` (new pawn tier 2 puzzle)
    - data/chessPuzzles.ts contains `capture-queen-3-1` (new queen tier 3 puzzle)
    - All 8 existing capture puzzle IDs still present
    - `npx tsx scripts/validate-puzzles.ts` reports 0 errors for ALL puzzles (movement + capture)
    - `npx tsx scripts/validate-puzzles.ts` reports 0 count warnings (all minimums met)
    - `npm run lint` passes
  </acceptance_criteria>
  <done>30 capture puzzles (8 existing + 22 new) pass validation, with at least 5 per piece and at least 10 per tier. Full pool is 90 puzzles (60 movement + 30 capture). Validation script exits with 0 errors and 0 warnings.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/validate-puzzles.ts` exits with code 0 — zero errors, zero warnings
- Movement puzzles: 60+ total, 10+ per piece, 10+ per tier
- Capture puzzles: 30+ total, 5+ per piece, 10+ per tier
- `npm run lint` passes
- `npm run build` passes (TypeScript compilation)
- All existing puzzle IDs preserved (no breaking changes)
- New puzzle IDs follow `{piece}-move-{tier}-{n}` and `capture-{piece}-{tier}-{n}` convention
</verification>

<success_criteria>
- 90+ total puzzles (60 movement + 30 capture) in data/chessPuzzles.ts
- Every puzzle validated by chess.js — correct legal moves, no ambiguous distractors
- 3 difficulty tiers with minimum 10 puzzles each in both movement and capture pools
- All 6 piece types well-represented (10 movement each, 5 capture each)
- Validation script passes with 0 errors and 0 count warnings
- Existing puzzles and helper functions unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/14-puzzle-pool-expansion/14-02-SUMMARY.md`
</output>
