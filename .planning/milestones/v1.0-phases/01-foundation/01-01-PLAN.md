---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/chessPieces.ts
  - messages/he.json
  - messages/en.json
  - messages/ru.json
autonomous: true
requirements:
  - INTG-03
  - INTG-06

must_haves:
  truths:
    - "chessPieces.ts exports an array of 6 chess pieces with Hebrew names, piece keys, and audio file paths"
    - "All 3 translation files contain chess piece names and game UI strings under a 'chessGame' key"
    - "Audio paths reference /audio/chess/he/*.mp3 and the code does not crash when files are missing"
  artifacts:
    - path: "data/chessPieces.ts"
      provides: "Chess piece data array with types"
      exports: ["ChessPieceConfig", "ChessPieceId", "chessPieces"]
    - path: "messages/he.json"
      provides: "Hebrew chess translations"
      contains: "chessGame"
    - path: "messages/en.json"
      provides: "English chess translations"
      contains: "chessGame"
    - path: "messages/ru.json"
      provides: "Russian chess translations"
      contains: "chessGame"
  key_links:
    - from: "data/chessPieces.ts"
      to: "messages/*.json"
      via: "translationKey field references chessGame.pieces.*"
      pattern: "chessGame\\.pieces\\."
    - from: "data/chessPieces.ts"
      to: "/public/audio/chess/he/*.mp3"
      via: "audioFile field references mp3 filenames"
      pattern: "audioFile.*\\.mp3"
---

<objective>
Create the chess piece data file with types, i18n translation keys for all 3 locales, and audio file path references.

Purpose: Downstream phases (board, game shell, levels) all consume piece data and translations. This plan establishes the data contracts everything else builds on.
Output: `data/chessPieces.ts` with types and data, translation keys in all 3 locale files.
</objective>

<execution_context>
@/Users/emil/code/lepdy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emil/code/lepdy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@data/letters.ts
@data/animals.ts
@messages/he.json
@messages/en.json
@messages/ru.json
@models/ModelsTypesEnum.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chess piece data file with types and audio paths</name>
  <files>data/chessPieces.ts</files>
  <read_first>
    - data/letters.ts (pattern reference for data file structure)
    - data/animals.ts (pattern reference for imageUrl/emoji field)
    - models/ModelsTypesEnum.ts (existing enum — do NOT modify, chess pieces use their own type)
  </read_first>
  <action>
Create `data/chessPieces.ts` with:

1. A `ChessPieceId` union type: `'king' | 'queen' | 'rook' | 'bishop' | 'knight' | 'pawn'`

2. A `ChessPieceConfig` interface:
```typescript
export interface ChessPieceConfig {
  id: ChessPieceId;
  translationKey: string;    // e.g. 'chessGame.pieces.king'
  audioFile: string;          // e.g. 'melech.mp3'
  audioPath: string;          // full path: '/audio/chess/he/melech.mp3'
  symbol: string;             // Unicode chess piece: '\u2654' for white king
  emoji: string;              // Display emoji: crown for king, etc.
  fenChar: string;            // FEN character: 'K', 'Q', 'R', 'B', 'N', 'P'
  color: string;              // Pastel color for card display
  order: number;              // Introduction order (1-6)
}
```

3. A `chessPieces` array with these 6 entries in introduction order (per INTRO-04: King, Rook, Bishop, Queen, Knight, Pawn):

| id | translationKey | audioFile | audioPath | symbol | emoji | fenChar | color | order |
|----|----------------|-----------|-----------|--------|-------|---------|-------|-------|
| king | chessGame.pieces.king | melech.mp3 | /audio/chess/he/melech.mp3 | \u2654 | -- (crown or similar) | K | #FFD700 | 1 |
| rook | chessGame.pieces.rook | tzariach.mp3 | /audio/chess/he/tzariach.mp3 | \u2656 | -- (castle or similar) | R | #87CEEB | 2 |
| bishop | chessGame.pieces.bishop | ratz.mp3 | /audio/chess/he/ratz.mp3 | \u2657 | -- | B | #DDA0DD | 3 |
| queen | chessGame.pieces.queen | malka.mp3 | /audio/chess/he/malka.mp3 | \u2655 | -- | Q | #FF69B4 | 4 |
| knight | chessGame.pieces.knight | parash.mp3 | /audio/chess/he/parash.mp3 | \u2658 | -- | N | #98FB98 | 5 |
| pawn | chessGame.pieces.pawn | chayal.mp3 | /audio/chess/he/chayal.mp3 | \u2659 | -- | P | #F0E68C | 6 |

Hebrew piece names used for audio filenames (from STATE.md blocker — these are from WordReference, may need verification):
- King = melech (מלך)
- Queen = malka (מלכה)
- Rook = tzariach (צריח)
- Bishop = ratz (רץ)
- Knight = parash (פרש)
- Pawn = chayal (חייל)

4. Export the type, interface, and array as named exports:
```typescript
export type { ChessPieceId };
export { chessPieces };
```

Do NOT import from or modify `ModelsTypesEnum.ts`. Chess pieces are a game concept, not a learning category — they use their own types.
  </action>
  <verify>
    <automated>grep -c "export" data/chessPieces.ts | grep -q "[2-9]" && grep "ChessPieceConfig" data/chessPieces.ts && grep "audioPath.*audio/chess/he" data/chessPieces.ts && grep "fenChar" data/chessPieces.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - data/chessPieces.ts contains `export interface ChessPieceConfig`
    - data/chessPieces.ts contains `export type ChessPieceId`
    - data/chessPieces.ts contains `export const chessPieces`
    - Array has exactly 6 entries (grep for `id:` returns 6 lines)
    - Each entry has `audioPath` containing `/audio/chess/he/`
    - Each entry has `fenChar` with one of K, Q, R, B, N, P
    - Each entry has `translationKey` starting with `chessGame.pieces.`
    - order values are 1 through 6
  </acceptance_criteria>
  <done>chessPieces.ts exists with 6 pieces, all typed, all with audio paths and FEN characters</done>
</task>

<task type="auto">
  <name>Task 2: Add chess translation keys to all 3 locale files</name>
  <files>messages/he.json, messages/en.json, messages/ru.json</files>
  <read_first>
    - messages/he.json (full file — must see current structure to add keys correctly)
    - messages/en.json (full file)
    - messages/ru.json (full file)
  </read_first>
  <action>
Add a top-level `"chessGame"` key to each locale file. Place it after the `"games"` key in the JSON structure for logical grouping.

**Hebrew (he.json) — add this object:**
```json
"chessGame": {
  "title": "שחמט",
  "pieces": {
    "king": "מלך",
    "queen": "מלכה",
    "rook": "צריח",
    "bishop": "רץ",
    "knight": "פרש",
    "pawn": "חייל"
  },
  "levels": {
    "pieceIntro": "הכר את הכלים",
    "movement": "לאן הכלי זז?",
    "capture": "מי יכול לתפוס?"
  },
  "ui": {
    "correct": "כל הכבוד!",
    "tryAgain": "נסו שוב!",
    "hint": "רמז",
    "next": "הבא",
    "back": "חזרה",
    "levelComplete": "השלב הושלם!",
    "tapToHear": "לחצו לשמוע",
    "findSquare": "מצאו את המשבצת הנכונה",
    "whichCaptures": "מי יכול לתפוס?"
  }
}
```

**English (en.json) — add this object:**
```json
"chessGame": {
  "title": "Chess",
  "pieces": {
    "king": "King",
    "queen": "Queen",
    "rook": "Rook",
    "bishop": "Bishop",
    "knight": "Knight",
    "pawn": "Pawn"
  },
  "levels": {
    "pieceIntro": "Meet the Pieces",
    "movement": "Where Can It Go?",
    "capture": "Who Can Capture?"
  },
  "ui": {
    "correct": "Great Job!",
    "tryAgain": "Try Again!",
    "hint": "Hint",
    "next": "Next",
    "back": "Back",
    "levelComplete": "Level Complete!",
    "tapToHear": "Tap to hear",
    "findSquare": "Find the right square",
    "whichCaptures": "Which piece can capture?"
  }
}
```

**Russian (ru.json) — add this object:**
```json
"chessGame": {
  "title": "Шахматы",
  "pieces": {
    "king": "Король",
    "queen": "Ферзь",
    "rook": "Ладья",
    "bishop": "Слон",
    "knight": "Конь",
    "pawn": "Пешка"
  },
  "levels": {
    "pieceIntro": "Знакомство с фигурами",
    "movement": "Куда может пойти?",
    "capture": "Кто может взять?"
  },
  "ui": {
    "correct": "Молодец!",
    "tryAgain": "Попробуй ещё!",
    "hint": "Подсказка",
    "next": "Дальше",
    "back": "Назад",
    "levelComplete": "Уровень пройден!",
    "tapToHear": "Нажми, чтобы послушать",
    "findSquare": "Найди правильную клетку",
    "whichCaptures": "Какая фигура может взять?"
  }
}
```

All 3 files must have IDENTICAL key structure under `chessGame` — same nesting, same keys, only values differ. This is critical for next-intl to work without missing key errors.
  </action>
  <verify>
    <automated>node -e "const he=require('./messages/he.json'); const en=require('./messages/en.json'); const ru=require('./messages/ru.json'); const keys=k=>JSON.stringify(Object.keys(k).sort()); if(!he.chessGame||!en.chessGame||!ru.chessGame){console.log('FAIL: missing chessGame'); process.exit(1);} if(keys(he.chessGame.pieces)!==keys(en.chessGame.pieces)||keys(en.chessGame.pieces)!==keys(ru.chessGame.pieces)){console.log('FAIL: piece keys mismatch'); process.exit(1);} if(keys(he.chessGame.ui)!==keys(en.chessGame.ui)||keys(en.chessGame.ui)!==keys(ru.chessGame.ui)){console.log('FAIL: ui keys mismatch'); process.exit(1);} console.log('PASS: all 3 locales have matching chessGame keys');"</automated>
  </verify>
  <acceptance_criteria>
    - messages/he.json contains `"chessGame"` top-level key
    - messages/en.json contains `"chessGame"` top-level key
    - messages/ru.json contains `"chessGame"` top-level key
    - Each chessGame has sub-keys: `pieces`, `levels`, `ui`
    - Each `pieces` has exactly 6 keys: king, queen, rook, bishop, knight, pawn
    - Hebrew pieces values are: מלך, מלכה, צריח, רץ, פרש, חייל
    - English pieces values are: King, Queen, Rook, Bishop, Knight, Pawn
    - Russian pieces values are: Король, Ферзь, Ладья, Слон, Конь, Пешка
    - All 3 locale files have identical key structure under chessGame (only values differ)
    - JSON is valid (node can require all 3 files without error)
  </acceptance_criteria>
  <done>All 3 locale files contain chessGame translations with matching key structures, piece names in all languages, and game UI strings</done>
</task>

</tasks>

<verification>
1. `data/chessPieces.ts` exports ChessPieceConfig interface, ChessPieceId type, and chessPieces array
2. Array contains exactly 6 pieces with ids: king, queen, rook, bishop, knight, pawn
3. All audioPath values follow pattern `/audio/chess/he/{name}.mp3`
4. All translationKey values follow pattern `chessGame.pieces.{id}`
5. All 3 translation files have `chessGame` with matching nested keys
6. `npm run build` passes (TypeScript compiles, no import errors)
</verification>

<success_criteria>
- chessPieces.ts is importable and exports typed data for all 6 chess pieces
- Translation keys in chessPieces.ts correspond to actual keys in all 3 locale files
- Audio paths reference /audio/chess/he/*.mp3 (files do not need to exist)
- Build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
