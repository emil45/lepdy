---
phase: 11-board-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[locale]/games/chess-game/MovementPuzzle.tsx
  - app/[locale]/games/chess-game/CapturePuzzle.tsx
autonomous: true
requirements: [BOARD-01, BOARD-02]

must_haves:
  truths:
    - "Board light squares display beigePastel (#f5ede1) instead of default tan"
    - "Board dark squares display purplePastel (#dbc3e2) instead of default brown"
    - "Coordinate labels (a-h, 1-8) are visible in blackPastel (#434243) at 50% opacity"
    - "Move highlight colors complement the pastel board (softer green/yellow, not default harsh colors)"
    - "Board looks consistent across both puzzle types (Movement and Capture)"
  artifacts:
    - path: "app/[locale]/games/chess-game/MovementPuzzle.tsx"
      provides: "Pastel board styling for movement puzzles"
      contains: "lightSquareStyle"
    - path: "app/[locale]/games/chess-game/CapturePuzzle.tsx"
      provides: "Pastel board styling for capture puzzles"
      contains: "darkSquareStyle"
  key_links:
    - from: "MovementPuzzle.tsx Chessboard options"
      to: "react-chessboard rendering"
      via: "lightSquareStyle, darkSquareStyle, notation style props"
      pattern: "lightSquareStyle.*#f5ede1"
    - from: "CapturePuzzle.tsx Chessboard options"
      to: "react-chessboard rendering"
      via: "lightSquareStyle, darkSquareStyle, notation style props"
      pattern: "darkSquareStyle.*#dbc3e2"
---

<objective>
Apply Lepdy's pastel color palette to the chess board squares and coordinate labels in both puzzle components.

Purpose: Replace the default react-chessboard brown/beige look with Lepdy's playful pastel style so the board feels visually consistent with the rest of the app.
Output: Both MovementPuzzle.tsx and CapturePuzzle.tsx render boards with pastel squares and complementary coordinate labels.
</objective>

<execution_context>
@/Users/emil/code/lepdy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emil/code/lepdy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-board-theme/11-CONTEXT.md

<interfaces>
<!-- react-chessboard ChessboardOptions props (from node_modules/react-chessboard/dist/ChessboardProvider.d.ts): -->

```typescript
// Square color props — React.CSSProperties objects
darkSquareStyle?: React.CSSProperties;
lightSquareStyle?: React.CSSProperties;

// Coordinate label styling — React.CSSProperties objects
darkSquareNotationStyle?: React.CSSProperties;   // Labels on dark squares
lightSquareNotationStyle?: React.CSSProperties;   // Labels on light squares
alphaNotationStyle?: React.CSSProperties;          // a-h file labels
numericNotationStyle?: React.CSSProperties;        // 1-8 rank labels
showNotation?: boolean;                            // Toggle coordinates

// Per-square overlay styles (already used for highlights)
squareStyles?: Record<string, React.CSSProperties>;
```

<!-- These props go inside the `options` object passed to `<Chessboard options={{...}}/>` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply pastel board colors and coordinate labels to MovementPuzzle</name>
  <files>app/[locale]/games/chess-game/MovementPuzzle.tsx</files>
  <read_first>
    - app/[locale]/games/chess-game/MovementPuzzle.tsx (current Chessboard usage at line ~242, squareStyles at line ~134)
    - theme/theme.ts (palette color values)
  </read_first>
  <action>
In MovementPuzzle.tsx, add four new props to the Chessboard `options` object (alongside existing `position`, `squareStyles`, etc.):

1. **Square colors (BOARD-01):**
   ```
   lightSquareStyle: { backgroundColor: '#f5ede1' },
   darkSquareStyle: { backgroundColor: '#dbc3e2' },
   ```

2. **Coordinate label colors (BOARD-02):**
   ```
   darkSquareNotationStyle: { color: 'rgba(67, 66, 67, 0.5)' },
   lightSquareNotationStyle: { color: 'rgba(67, 66, 67, 0.5)' },
   ```
   (blackPastel #434243 = rgb(67,66,67) at 50% opacity)

3. **Update highlight colors in `squareStyles` useMemo** to complement pastel board:
   - Change the piece's current square highlight from `rgba(255, 255, 0, 0.4)` (harsh yellow) to `rgba(255, 223, 100, 0.45)` (soft gold)
   - Change the hint dot color from `rgba(0,128,0,0.4)` to `rgba(76, 175, 80, 0.45)` (softer green that works on both pastel squares)
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && grep -c "lightSquareStyle" app/\[locale\]/games/chess-game/MovementPuzzle.tsx | grep -q "1" && grep -c "darkSquareStyle" app/\[locale\]/games/chess-game/MovementPuzzle.tsx | grep -q "1" && grep -c "darkSquareNotationStyle" app/\[locale\]/games/chess-game/MovementPuzzle.tsx | grep -q "1" && grep -c "lightSquareNotationStyle" app/\[locale\]/games/chess-game/MovementPuzzle.tsx | grep -q "1" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - MovementPuzzle.tsx contains `lightSquareStyle: { backgroundColor: '#f5ede1' }`
    - MovementPuzzle.tsx contains `darkSquareStyle: { backgroundColor: '#dbc3e2' }`
    - MovementPuzzle.tsx contains `darkSquareNotationStyle: { color: 'rgba(67, 66, 67, 0.5)' }`
    - MovementPuzzle.tsx contains `lightSquareNotationStyle: { color: 'rgba(67, 66, 67, 0.5)' }`
    - MovementPuzzle.tsx squareStyles uses `rgba(255, 223, 100, 0.45)` for piece square highlight (not `rgba(255, 255, 0, 0.4)`)
    - MovementPuzzle.tsx squareStyles uses `rgba(76, 175, 80, 0.45)` for hint dots (not `rgba(0,128,0,0.4)`)
  </acceptance_criteria>
  <done>MovementPuzzle board renders with pastel squares, soft coordinate labels, and complementary highlight colors</done>
</task>

<task type="auto">
  <name>Task 2: Apply identical pastel board colors and coordinate labels to CapturePuzzle</name>
  <files>app/[locale]/games/chess-game/CapturePuzzle.tsx</files>
  <read_first>
    - app/[locale]/games/chess-game/CapturePuzzle.tsx (current Chessboard usage at line ~227, squareStyles at line ~124)
    - app/[locale]/games/chess-game/MovementPuzzle.tsx (reference — Task 1 output to match exactly)
  </read_first>
  <action>
In CapturePuzzle.tsx, add the same four props to the Chessboard `options` object:

1. **Square colors (BOARD-01):**
   ```
   lightSquareStyle: { backgroundColor: '#f5ede1' },
   darkSquareStyle: { backgroundColor: '#dbc3e2' },
   ```

2. **Coordinate label colors (BOARD-02):**
   ```
   darkSquareNotationStyle: { color: 'rgba(67, 66, 67, 0.5)' },
   lightSquareNotationStyle: { color: 'rgba(67, 66, 67, 0.5)' },
   ```

3. **Update highlight colors in `squareStyles` useMemo** to complement pastel board:
   - Change the target square box-shadow from `rgba(220, 60, 0, 0.85)` to `rgba(220, 80, 40, 0.7)` (slightly softer red-orange that still reads clearly on pastel)
   - Change the hint square box-shadow from `rgba(0, 180, 0, 0.85)` to `rgba(76, 175, 80, 0.7)` (softer green matching MovementPuzzle's green family)
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && grep -c "lightSquareStyle" app/\[locale\]/games/chess-game/CapturePuzzle.tsx | grep -q "1" && grep -c "darkSquareStyle" app/\[locale\]/games/chess-game/CapturePuzzle.tsx | grep -q "1" && grep -c "darkSquareNotationStyle" app/\[locale\]/games/chess-game/CapturePuzzle.tsx | grep -q "1" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - CapturePuzzle.tsx contains `lightSquareStyle: { backgroundColor: '#f5ede1' }`
    - CapturePuzzle.tsx contains `darkSquareStyle: { backgroundColor: '#dbc3e2' }`
    - CapturePuzzle.tsx contains `darkSquareNotationStyle: { color: 'rgba(67, 66, 67, 0.5)' }`
    - CapturePuzzle.tsx contains `lightSquareNotationStyle: { color: 'rgba(67, 66, 67, 0.5)' }`
    - CapturePuzzle.tsx squareStyles uses `rgba(220, 80, 40, 0.7)` for target square (not `rgba(220, 60, 0, 0.85)`)
    - CapturePuzzle.tsx squareStyles uses `rgba(76, 175, 80, 0.7)` for hint square (not `rgba(0, 180, 0, 0.85)`)
  </acceptance_criteria>
  <done>CapturePuzzle board renders with identical pastel squares, coordinate labels, and complementary highlight colors as MovementPuzzle</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Both puzzle components contain lightSquareStyle, darkSquareStyle, darkSquareNotationStyle, lightSquareNotationStyle
3. No references to the old default highlight colors remain in either file
</verification>

<success_criteria>
- Both MovementPuzzle and CapturePuzzle boards display beigePastel light squares and purplePastel dark squares
- Coordinate labels show in blackPastel at 50% opacity on both square types
- Highlight overlays (selected square, hints, target) use softer colors that complement the pastel palette
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-board-theme/11-01-SUMMARY.md`
</output>
