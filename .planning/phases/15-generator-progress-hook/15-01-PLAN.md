---
phase: 15-generator-progress-hook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/puzzleGenerator.ts
  - hooks/usePuzzleProgress.ts
  - lib/featureFlags/types.ts
  - lib/featureFlags/providers/firebaseRemoteConfig.ts
autonomous: true
requirements: [PGEN-03, DIFF-01, DIFF-02, DIFF-03]

must_haves:
  truths:
    - "selectNextPuzzle filters by difficulty tier and excludes recently seen puzzle IDs"
    - "Seen-window never exceeds 15 entries and resets gracefully when tier pool exhausted"
    - "usePuzzleProgress tracks per-piece tier and consecutive correct/wrong streaks in localStorage"
    - "Tier advances (capped at 3) after consecutive correct threshold, de-escalates (floored at 1) after consecutive wrong threshold"
    - "Difficulty thresholds are read from Firebase Remote Config feature flags, not hardcoded"
  artifacts:
    - path: "utils/puzzleGenerator.ts"
      provides: "Pure puzzle selection with dedup sliding window"
      exports: ["selectNextPuzzle", "defaultGeneratorState", "GeneratorState"]
    - path: "hooks/usePuzzleProgress.ts"
      provides: "Per-piece difficulty tier and streak persistence"
      exports: ["usePuzzleProgress", "PuzzleProgressData", "PiecePuzzleProgress"]
    - path: "lib/featureFlags/types.ts"
      provides: "chessAdvanceTierThreshold and chessDemoTierThreshold flag definitions"
      contains: "chessAdvanceTierThreshold"
    - path: "lib/featureFlags/providers/firebaseRemoteConfig.ts"
      provides: "Remote Config fetch for difficulty threshold flags"
      contains: "chessAdvanceTierThreshold"
  key_links:
    - from: "hooks/usePuzzleProgress.ts"
      to: "lib/featureFlags/types.ts"
      via: "useFeatureFlagContext().getFlag"
      pattern: "getFlag.*chessAdvanceTierThreshold"
    - from: "hooks/usePuzzleProgress.ts"
      to: "localStorage"
      via: "lepdy_chess_puzzle_progress storage key"
      pattern: "lepdy_chess_puzzle_progress"
---

<objective>
Create the puzzle generator utility and puzzle progress hook that power infinite replayability.

Purpose: These two artifacts are the engine behind random puzzle selection (with dedup) and adaptive difficulty (per-piece tier tracking). Phase 16 wires them into the UI; this phase builds and exports them.

Output: `utils/puzzleGenerator.ts` (pure functions), `hooks/usePuzzleProgress.ts` (React hook), updated feature flag types and provider.
</objective>

<execution_context>
@/Users/emil/code/lepdy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emil/code/lepdy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From data/chessPuzzles.ts:
```typescript
export interface MovementPuzzle {
  id: string;
  pieceId: ChessPieceId;
  fen: string;
  pieceSquare: string;
  validTargets: string[];
  difficulty: 1 | 2 | 3;
}

export interface CapturePuzzle {
  id: string;
  fen: string;
  targetSquare: string;
  correctPieceSquare: string;
  correctPieceId: ChessPieceId;
  targetPieceId: ChessPieceId;
  distractorSquares: string[];
  difficulty: 1 | 2 | 3;
}
```

From data/chessPieces.ts:
```typescript
export type ChessPieceId = 'king' | 'queen' | 'rook' | 'bishop' | 'knight' | 'pawn';
```

From lib/featureFlags/types.ts:
```typescript
export interface FeatureFlags {
  showStickersButton: boolean;
  showVoiceIndicator: boolean;
  soundMatchingWrongAnswerDelayMs: number;
}

export const DEFAULT_FLAGS: FeatureFlags = {
  showStickersButton: false,
  showVoiceIndicator: false,
  soundMatchingWrongAnswerDelayMs: 2500,
};
```

From hooks/useChessProgress.ts (pattern to follow):
```typescript
// SSR guard: typeof window !== 'undefined'
// useState + useEffect load on mount + useEffect save on change
// useCallback for mutations
// STORAGE_KEY pattern: 'lepdy_chess_progress'
```

From contexts/FeatureFlagContext.tsx (consumer pattern):
```typescript
const { getFlag } = useFeatureFlagContext();
// getFlag('chessAdvanceTierThreshold') returns number
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create puzzle generator utility and add feature flag config</name>
  <files>utils/puzzleGenerator.ts, lib/featureFlags/types.ts, lib/featureFlags/providers/firebaseRemoteConfig.ts</files>
  <read_first>
    - data/chessPuzzles.ts (lines 1-25 for interfaces and puzzle shape)
    - data/chessPieces.ts (ChessPieceId type)
    - lib/featureFlags/types.ts (existing FeatureFlags interface and DEFAULT_FLAGS)
    - lib/featureFlags/providers/firebaseRemoteConfig.ts (fetchFlags method, getBooleanFlag/getNumberFlag patterns)
  </read_first>
  <action>
    **1. Create `utils/puzzleGenerator.ts`** — pure TypeScript, NO React import, NO chess.js import, NO browser APIs.

    Export `GeneratorState` interface with `seenIds: string[]` (max 15 entries).

    Export `defaultGeneratorState()` returning `{ seenIds: [] }`.

    Export `selectNextPuzzle<T extends { id: string; difficulty: 1 | 2 | 3 }>(pool: T[], tier: 1 | 2 | 3, state: GeneratorState): { puzzle: T; nextState: GeneratorState }`:
    - Filter pool to items matching `tier`
    - From tier pool, exclude items whose `id` is in `state.seenIds`
    - If eligible list is empty (all tier puzzles seen), use the full tier pool (reset dedup for this tier)
    - Pick random item: `source[Math.floor(Math.random() * source.length)]`
    - Build nextState: `seenIds = [...state.seenIds, puzzle.id].slice(-15)` (ring buffer, max 15)
    - Return `{ puzzle, nextState }`

    **2. Update `lib/featureFlags/types.ts`**:
    - Add to `FeatureFlags` interface: `chessAdvanceTierThreshold: number;` and `chessDemoTierThreshold: number;`
    - Add to `DEFAULT_FLAGS`: `chessAdvanceTierThreshold: 5,` and `chessDemoTierThreshold: 3,`

    **3. Update `lib/featureFlags/providers/firebaseRemoteConfig.ts`**:
    - In the `fetchFlags()` method, add two lines to the `newFlags` object inside the try block:
      `chessAdvanceTierThreshold: this.getNumberFlag('chessAdvanceTierThreshold', getValue),`
      `chessDemoTierThreshold: this.getNumberFlag('chessDemoTierThreshold', getValue),`
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "selectNextPuzzle" utils/puzzleGenerator.ts` returns >= 1
    - `grep -c "defaultGeneratorState" utils/puzzleGenerator.ts` returns >= 1
    - `grep -c "GeneratorState" utils/puzzleGenerator.ts` returns >= 2
    - `grep -c "seenIds" utils/puzzleGenerator.ts` returns >= 3
    - `grep -c "slice(-15)" utils/puzzleGenerator.ts` returns >= 1
    - `grep -c "chessAdvanceTierThreshold" lib/featureFlags/types.ts` returns >= 2 (interface + default)
    - `grep -c "chessDemoTierThreshold" lib/featureFlags/types.ts` returns >= 2
    - `grep -c "chessAdvanceTierThreshold" lib/featureFlags/providers/firebaseRemoteConfig.ts` returns >= 1
    - `grep -c "chessDemoTierThreshold" lib/featureFlags/providers/firebaseRemoteConfig.ts` returns >= 1
    - No `import.*react` in utils/puzzleGenerator.ts
    - No `import.*chess` in utils/puzzleGenerator.ts
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>puzzleGenerator.ts exports selectNextPuzzle, defaultGeneratorState, and GeneratorState. Feature flags define chessAdvanceTierThreshold (default 5) and chessDemoTierThreshold (default 3) and firebaseRemoteConfig.ts fetches both.</done>
</task>

<task type="auto">
  <name>Task 2: Create usePuzzleProgress hook with per-piece tier tracking</name>
  <files>hooks/usePuzzleProgress.ts</files>
  <read_first>
    - hooks/useChessProgress.ts (full file — copy the SSR guard, load/save, useCallback pattern exactly)
    - lib/featureFlags/types.ts (verify chessAdvanceTierThreshold and chessDemoTierThreshold exist from Task 1)
    - contexts/FeatureFlagContext.tsx (useFeatureFlagContext import path and getFlag usage)
    - data/chessPieces.ts (ChessPieceId type import)
  </read_first>
  <action>
    Create `hooks/usePuzzleProgress.ts` with `'use client'` directive.

    **Constants:**
    - `STORAGE_KEY = 'lepdy_chess_puzzle_progress'`
    - `DEFAULT_PIECE_PROGRESS: PiecePuzzleProgress = { tier: 1, consecutiveCorrect: 0, consecutiveWrong: 0 }`

    **Exported interfaces:**
    - `PiecePuzzleProgress`: `{ tier: 1 | 2 | 3; consecutiveCorrect: number; consecutiveWrong: number; }`
    - `PuzzleProgressData`: `{ pieces: Record<string, PiecePuzzleProgress>; }` (string keys are ChessPieceId values)
    - `UsePuzzleProgressReturn`: `{ getSessionTier, recordCorrect, recordWrong, data }`

    **Hook implementation — follow useChessProgress pattern exactly:**
    1. `useState<PuzzleProgressData>` initialized to `{ pieces: {} }`
    2. `useState<boolean>(false)` for `isInitialized`
    3. Import `useFeatureFlagContext` from `@/contexts/FeatureFlagContext` and destructure `getFlag`
    4. `useEffect` on mount: SSR guard (`typeof window !== 'undefined'`), try/catch `localStorage.getItem(STORAGE_KEY)`, parse JSON, validate `pieces` is an object, `setData`, then `setIsInitialized(true)`. Log errors with `console.error('[chess] Failed to load puzzle progress:', e)`
    5. `useEffect` on `[data, isInitialized]`: if not initialized, return. try/catch `localStorage.setItem(STORAGE_KEY, JSON.stringify(data))`. Log errors.
    6. `getSessionTier = useCallback((pieceId: ChessPieceId): 1 | 2 | 3 => (data.pieces[pieceId] ?? DEFAULT_PIECE_PROGRESS).tier, [data.pieces])`
    7. `recordCorrect = useCallback((pieceId: ChessPieceId) => { setData(prev => { ... }) }, [getFlag])`:
       - Get `current = prev.pieces[pieceId] ?? DEFAULT_PIECE_PROGRESS`
       - `nextCorrect = current.consecutiveCorrect + 1`
       - `advanceThreshold = getFlag('chessAdvanceTierThreshold') as number`
       - `shouldAdvance = nextCorrect >= advanceThreshold`
       - Return new data with piece updated: `tier: shouldAdvance ? Math.min(3, current.tier + 1) as 1|2|3 : current.tier`, `consecutiveCorrect: shouldAdvance ? 0 : nextCorrect`, `consecutiveWrong: 0` (always reset wrong on correct)
    8. `recordWrong = useCallback((pieceId: ChessPieceId) => { setData(prev => { ... }) }, [getFlag])`:
       - Get `current = prev.pieces[pieceId] ?? DEFAULT_PIECE_PROGRESS`
       - `nextWrong = current.consecutiveWrong + 1`
       - `demoThreshold = getFlag('chessDemoTierThreshold') as number`
       - `shouldDemote = nextWrong >= demoThreshold`
       - Return: `tier: shouldDemote ? Math.max(1, current.tier - 1) as 1|2|3 : current.tier`, `consecutiveWrong: shouldDemote ? 0 : nextWrong`, `consecutiveCorrect: 0` (always reset correct on wrong)
    9. Return `{ getSessionTier, recordCorrect, recordWrong, data }`
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lepdy_chess_puzzle_progress" hooks/usePuzzleProgress.ts` returns >= 1
    - `grep -c "getSessionTier" hooks/usePuzzleProgress.ts` returns >= 2 (definition + return)
    - `grep -c "recordCorrect" hooks/usePuzzleProgress.ts` returns >= 2
    - `grep -c "recordWrong" hooks/usePuzzleProgress.ts` returns >= 2
    - `grep -c "chessAdvanceTierThreshold" hooks/usePuzzleProgress.ts` returns >= 1
    - `grep -c "chessDemoTierThreshold" hooks/usePuzzleProgress.ts` returns >= 1
    - `grep -c "consecutiveCorrect" hooks/usePuzzleProgress.ts` returns >= 3
    - `grep -c "consecutiveWrong" hooks/usePuzzleProgress.ts` returns >= 3
    - `grep "Math.min(3" hooks/usePuzzleProgress.ts` matches (tier cap)
    - `grep "Math.max(1" hooks/usePuzzleProgress.ts` matches (tier floor)
    - `grep "use client" hooks/usePuzzleProgress.ts` matches
    - `grep "typeof window" hooks/usePuzzleProgress.ts` matches (SSR guard)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>usePuzzleProgress hook persists per-piece tier (1-3) and consecutive answer streaks in localStorage. getSessionTier returns locked tier for session start. recordCorrect advances tier after chessAdvanceTierThreshold consecutive correct. recordWrong de-escalates after chessDemoTierThreshold consecutive wrong. Both thresholds read from feature flags.</done>
</task>

</tasks>

<verification>
- `npm run lint` passes with zero errors
- `utils/puzzleGenerator.ts` has no React or chess.js imports (pure TypeScript)
- `hooks/usePuzzleProgress.ts` follows useChessProgress SSR guard pattern
- Feature flag types include both threshold flags with correct defaults
- firebaseRemoteConfig.ts fetches both new flags
</verification>

<success_criteria>
1. selectNextPuzzle correctly filters by tier, excludes seen IDs, returns random puzzle with updated sliding window
2. usePuzzleProgress tracks per-piece (not global) tier and streaks
3. Tier advances at 5 consecutive correct (configurable via Remote Config)
4. Tier de-escalates at 3 consecutive wrong (configurable via Remote Config)
5. All code type-checks cleanly (npm run lint)
</success_criteria>

<output>
After completion, create `.planning/phases/15-generator-progress-hook/15-01-SUMMARY.md`
</output>
