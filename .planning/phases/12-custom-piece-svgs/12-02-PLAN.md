---
phase: 12-custom-piece-svgs
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - public/chess/pieces/horsey/wK.svg
  - public/chess/pieces/horsey/wQ.svg
  - public/chess/pieces/horsey/wR.svg
  - public/chess/pieces/horsey/wB.svg
  - public/chess/pieces/horsey/wN.svg
  - public/chess/pieces/horsey/wP.svg
  - public/chess/pieces/horsey/bK.svg
  - public/chess/pieces/horsey/bQ.svg
  - public/chess/pieces/horsey/bR.svg
  - public/chess/pieces/horsey/bB.svg
  - public/chess/pieces/horsey/bN.svg
  - public/chess/pieces/horsey/bP.svg
  - app/[locale]/games/chess-game/pieceThemes.tsx
autonomous: false
requirements: [PIECE-02]

must_haves:
  truths:
    - "All 12 horsey pieces (6 white + 6 black) render on the chess board when horsey theme is selected"
    - "Horsey pieces are visually distinct from each other at 320px board width"
    - "Switching between staunty and horsey themes works via useChessPieceTheme hook"
  artifacts:
    - path: "public/chess/pieces/horsey/"
      provides: "12 SVG files for horsey theme"
    - path: "app/[locale]/games/chess-game/pieceThemes.tsx"
      provides: "Updated registry with horsey entry"
      contains: "horsey: buildPieceRenderObject"
  key_links:
    - from: "app/[locale]/games/chess-game/pieceThemes.tsx"
      to: "public/chess/pieces/horsey/"
      via: "buildPieceRenderObject('horsey') generates paths to /chess/pieces/horsey/*.svg"
      pattern: "horsey.*buildPieceRenderObject"
---

<objective>
Add horsey SVGs as a second registered theme and visually verify responsive rendering of both themes.

Purpose: Deliver the second kid-friendly piece set and validate that the extensible theme system works with multiple themes.
Output: 12 horsey SVGs, updated pieceThemes.tsx with horsey entry, visual verification of both themes at responsive sizes.
</objective>

<execution_context>
@/Users/emil/code/lepdy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emil/code/lepdy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-custom-piece-svgs/12-CONTEXT.md
@.planning/phases/12-custom-piece-svgs/12-RESEARCH.md
@.planning/phases/12-custom-piece-svgs/12-01-SUMMARY.md

<interfaces>
<!-- From plan 01 outputs -->

From app/[locale]/games/chess-game/pieceThemes.tsx (created in plan 01):
```typescript
export type ThemeName = 'staunty' | 'horsey';
export const PIECE_CODES = ['wK','wQ','wR','wB','wN','wP','bK','bQ','bR','bB','bN','bP'] as const;
function buildPieceRenderObject(theme: string): PieceRenderObject { ... }
export const pieceThemes: Record<ThemeName, PieceRenderObject> = {
  staunty: buildPieceRenderObject('staunty'),
  // horsey entry to be added in this plan
};
```

From hooks/useChessPieceTheme.ts (created in plan 01):
```typescript
export function useChessPieceTheme(): { theme: ThemeName; pieces: PieceRenderObject; selectTheme: (name: ThemeName) => void }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download horsey SVGs and register theme</name>
  <files>
    public/chess/pieces/horsey/wK.svg
    public/chess/pieces/horsey/wQ.svg
    public/chess/pieces/horsey/wR.svg
    public/chess/pieces/horsey/wB.svg
    public/chess/pieces/horsey/wN.svg
    public/chess/pieces/horsey/wP.svg
    public/chess/pieces/horsey/bK.svg
    public/chess/pieces/horsey/bQ.svg
    public/chess/pieces/horsey/bR.svg
    public/chess/pieces/horsey/bB.svg
    public/chess/pieces/horsey/bN.svg
    public/chess/pieces/horsey/bP.svg
    app/[locale]/games/chess-game/pieceThemes.tsx
  </files>
  <read_first>
    app/[locale]/games/chess-game/pieceThemes.tsx
  </read_first>
  <action>
    **Step 1: Download horsey SVGs**

    ```bash
    mkdir -p public/chess/pieces/horsey
    cd public/chess/pieces/horsey
    for piece in wK wQ wR wB wN wP bK bQ bR bB bN bP; do
      curl -sS -o "${piece}.svg" "https://raw.githubusercontent.com/lichess-org/lila/master/public/piece/horsey/${piece}.svg"
    done
    ```

    Verify all 12 files downloaded and each contains `<svg` and `viewBox`:
    ```bash
    ls public/chess/pieces/horsey/*.svg | wc -l  # must be 12
    grep -l 'viewBox' public/chess/pieces/horsey/*.svg | wc -l  # must be 12
    ```

    **Step 2: Add horsey to pieceThemes.tsx**

    In the `pieceThemes` export object, add the horsey entry:
    ```typescript
    export const pieceThemes: Record<ThemeName, PieceRenderObject> = {
      staunty: buildPieceRenderObject('staunty'),
      horsey: buildPieceRenderObject('horsey'),  // <-- add this line
    };
    ```

    This is the ONLY code change needed — the factory function and ThemeName type already support horsey from plan 01. This proves PIECE-04 (extensibility).
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && ls public/chess/pieces/horsey/*.svg | wc -l && grep -c "horsey" app/\[locale\]/games/chess-game/pieceThemes.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 12 SVG files exist in public/chess/pieces/horsey/ (wK.svg through bP.svg)
    - Each SVG contains `<svg` and `viewBox`
    - pieceThemes.tsx contains `horsey: buildPieceRenderObject('horsey')`
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>12 horsey SVGs downloaded, horsey entry added to pieceThemes registry, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify both themes render correctly at responsive sizes</name>
  <files>none</files>
  <read_first>
    app/[locale]/games/chess-game/pieceThemes.tsx
    hooks/useChessPieceTheme.ts
  </read_first>
  <action>
    Start the dev server with `npm run dev`. Present the user with the verification steps below. No code changes in this task — this is a visual verification checkpoint.
  </action>
  <what-built>Two complete piece themes (staunty + horsey) integrated into all chess game components via the extensible theme architecture</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/games/chess-game
    2. Click Level 1 (Piece Introduction) — verify piece card shows staunty SVG image (not Unicode symbol)
    3. Press Back, click Level 2 (Movement Puzzles) — verify board shows staunty SVG pieces on pastel squares
    4. Press Back, click Level 3 (if unlocked, otherwise set localStorage `lepdy_chess_progress` to `{"completedLevels":[1,2],"currentLevel":3}`) — verify capture puzzle shows staunty pieces
    5. Open browser DevTools Console, run: `localStorage.setItem('lepdy_chess_piece_theme', 'horsey')`
    6. Refresh the page and repeat steps 2-4 — all pieces should now show horsey (cartoon) style
    7. Resize browser window to ~320px width — verify pieces are distinct and not clipped at small size
    8. Resize to ~480px width — verify pieces look good at larger size
    9. Verify each piece type is visually distinguishable in both themes at both sizes
  </how-to-verify>
  <verify>Visual inspection by user — no automated command</verify>
  <resume-signal>Type "approved" if both themes render correctly at both sizes, or describe any issues</resume-signal>
  <acceptance_criteria>
    - Staunty pieces visible on all 3 chess game screens (no default react-chessboard pieces)
    - Horsey pieces visible on all 3 screens when theme is set to horsey
    - All 6 piece types distinguishable at 320px board width in both themes
    - No clipping or distortion at either size
  </acceptance_criteria>
  <done>Both staunty and horsey themes render correctly across all chess components at 320px and 480px board widths</done>
</task>

</tasks>

<verification>
1. `ls public/chess/pieces/horsey/*.svg | wc -l` returns 12
2. `npm run build` passes
3. `npm test -- --grep "chess"` passes — existing chess E2E tests green
4. Visual verification of both themes at 320px and 480px board widths
</verification>

<success_criteria>
- Horsey SVG pieces render on all chess boards when horsey theme selected
- Both themes are visually distinct and kid-appropriate at all supported sizes
- The theme was added with only 1 line of code change + SVG files (proving extensibility)
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-custom-piece-svgs/12-02-SUMMARY.md`
</output>
