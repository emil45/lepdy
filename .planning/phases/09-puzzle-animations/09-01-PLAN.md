---
phase: 09-puzzle-animations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/chessFen.ts
  - app/[locale]/games/chess-game/MovementPuzzle.tsx
  - app/[locale]/games/chess-game/CapturePuzzle.tsx
autonomous: true
requirements: [PFEED-01, PFEED-02]

must_haves:
  truths:
    - "When a child taps the correct movement square, the piece visibly slides from its origin to that square before confetti fires"
    - "When a child taps the correct capturing piece, that piece visibly slides to the target square (replacing the captured piece) before confetti fires"
    - "The slide animation completes in ~200ms and does not block the next puzzle from loading"
  artifacts:
    - path: "utils/chessFen.ts"
      provides: "FEN string manipulation — move a piece from one square to another"
      exports: ["moveFenPiece"]
    - path: "app/[locale]/games/chess-game/MovementPuzzle.tsx"
      provides: "Movement puzzle with piece slide animation on correct tap"
      contains: "moveFenPiece"
    - path: "app/[locale]/games/chess-game/CapturePuzzle.tsx"
      provides: "Capture puzzle with piece slide animation on correct tap"
      contains: "moveFenPiece"
  key_links:
    - from: "MovementPuzzle.tsx"
      to: "Chessboard position prop"
      via: "displayFen state replaces puzzle.fen"
      pattern: "position:\\s*displayFen"
    - from: "CapturePuzzle.tsx"
      to: "Chessboard position prop"
      via: "displayFen state replaces puzzle.fen"
      pattern: "position:\\s*displayFen"
    - from: "moveFenPiece"
      to: "FEN string"
      via: "square-to-FEN-index conversion"
      pattern: "moveFenPiece"
---

<objective>
Add piece-sliding animations to both movement and capture puzzles. When a child taps the correct answer, the piece visibly moves to the target square before the celebration fires.

Purpose: Correct answers currently flash green + confetti but the piece stays in place. Showing the piece physically move makes the puzzle feel complete and teaches correct chess movement visually.
Output: Both puzzle components animate the piece on correct tap using react-chessboard's built-in animation (triggered by updating the FEN position prop).
</objective>

<execution_context>
@/Users/emil/code/lepdy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emil/code/lepdy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-puzzle-animations/09-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From data/chessPuzzles.ts:
```typescript
export interface MovementPuzzle {
  id: string;
  pieceId: ChessPieceId;
  fen: string;           // e.g. "8/8/8/8/4R3/8/8/8"
  pieceSquare: string;    // e.g. "e4" — origin square
  validTargets: string[]; // e.g. ["e1","e2",...] — correct destination squares
  difficulty: 1 | 2 | 3;
}

export interface CapturePuzzle {
  id: string;
  fen: string;
  targetSquare: string;        // square of the piece to be captured
  correctPieceSquare: string;  // square of the capturing piece
  correctPieceId: ChessPieceId;
  targetPieceId: ChessPieceId;
  distractorSquares: string[];
  difficulty: 1 | 2 | 3;
}
```

From react-chessboard Chessboard component:
- `position` prop accepts a FEN piece-placement string (just the first field, e.g. "8/8/8/8/4R3/8/8/8")
- When `position` changes, the component automatically animates piece movement over `animationDurationInMs` (currently 200ms)
- Both puzzles already pass `animationDurationInMs: 200` in options
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FEN manipulation helper and animate MovementPuzzle</name>
  <files>utils/chessFen.ts, utils/chessFen.test.ts, app/[locale]/games/chess-game/MovementPuzzle.tsx</files>
  <read_first>
    @app/[locale]/games/chess-game/MovementPuzzle.tsx
    @data/chessPuzzles.ts (lines 1-25 for interfaces)
  </read_first>
  <behavior>
    - moveFenPiece("8/8/8/8/4R3/8/8/8", "e4", "e7") returns "8/4R3/8/8/8/8/8/8" (rook moved from e4 to e7)
    - moveFenPiece("8/8/8/8/4R3/8/8/8", "e4", "a4") returns "8/8/8/8/R7/8/8/8" (rook moved from e4 to a4)
    - moveFenPiece with from-square having no piece throws or returns original FEN unchanged
    - moveFenPiece("rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR", "e2", "e4") returns "rnbqkbnr/pppppppp/8/8/4P3/8/PPPP1PPP/RNBQKBNR"
    - moveFenPiece handles capture: destination square piece is replaced by source piece
  </behavior>
  <action>
    **Step 1 — Create `utils/chessFen.ts`:**

    Export a function `moveFenPiece(fen: string, fromSquare: string, toSquare: string): string`.

    Implementation approach:
    1. Parse FEN string into an 8x8 grid (split by "/", expand digit shorthand into dots or similar)
    2. Convert algebraic square notation (e.g. "e4") to row/col indices: col = "abcdefgh".indexOf(char), row = 8 - parseInt(rank)
    3. Get the piece character at fromSquare. If empty, return fen unchanged.
    4. Clear fromSquare (set to empty)
    5. Place piece character at toSquare (replaces whatever was there — handles captures)
    6. Collapse the grid back into FEN notation (consecutive empties become digits)
    7. Return the new FEN string

    **Step 2 — Create `utils/chessFen.test.ts`:**

    Test the behavior cases listed above using vitest or jest (check which test runner the project uses — the project uses Playwright for E2E but may not have a unit test runner; if not, create a simple Node test script that can run with `npx tsx`).

    **Step 3 — Update `MovementPuzzle.tsx`:**

    a. Import `moveFenPiece` from `@/utils/chessFen`
    b. Add state: `const [displayFen, setDisplayFen] = useState(puzzle.fen)`
    c. Reset displayFen when puzzleIndex changes: add a `useEffect` that runs `setDisplayFen(puzzle.fen)` when `puzzleIndex` changes (or derive from puzzle directly)
    d. In the correct-tap branch of `handlePuzzleSquareClick` (line ~81-100), BEFORE the existing setTimeout:
       - Set `isAdvancing(true)` FIRST (already done at line 87 — keep it)
       - Compute new FEN: `const newFen = moveFenPiece(puzzle.fen, puzzle.pieceSquare, square)`
       - Call `setDisplayFen(newFen)` — this triggers react-chessboard's 200ms slide animation
       - Keep the green flash and confetti as-is (they fire simultaneously with the slide)
       - The existing 1500ms setTimeout remains unchanged — gives 200ms slide + 1300ms celebration viewing
    e. Change the Chessboard `position` prop from `puzzle.fen` to `displayFen`
    f. Keep `animationDurationInMs: 200` as-is

    Timing sequence after this change:
    - t=0ms: Tap correct square → isAdvancing=true, displayFen updated, green flash, confetti, celebration sound
    - t=0-200ms: Piece slides to target (react-chessboard built-in animation)
    - t=1500ms: Advance to next puzzle or complete level
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && npx tsx utils/chessFen.test.ts 2>/dev/null || node --import tsx utils/chessFen.test.ts 2>/dev/null || echo "Run unit tests manually"; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "moveFenPiece" utils/chessFen.ts (helper exists)
    - grep -q "moveFenPiece" app/\[locale\]/games/chess-game/MovementPuzzle.tsx (imported and used)
    - grep -q "displayFen" app/\[locale\]/games/chess-game/MovementPuzzle.tsx (new state drives board position)
    - grep -q "position.*displayFen" app/\[locale\]/games/chess-game/MovementPuzzle.tsx (Chessboard reads displayFen)
    - npm run build succeeds without errors
  </acceptance_criteria>
  <done>MovementPuzzle shows the piece sliding to the tapped square on correct answer. FEN helper is tested and reusable.</done>
</task>

<task type="auto">
  <name>Task 2: Animate CapturePuzzle with the same FEN helper</name>
  <files>app/[locale]/games/chess-game/CapturePuzzle.tsx</files>
  <read_first>
    @app/[locale]/games/chess-game/CapturePuzzle.tsx
    @utils/chessFen.ts (created in Task 1)
  </read_first>
  <action>
    Apply the same animation pattern from Task 1 to CapturePuzzle.tsx (per PFEED-02):

    a. Import `moveFenPiece` from `@/utils/chessFen`
    b. Add state: `const [displayFen, setDisplayFen] = useState(puzzle.fen)`
    c. Reset displayFen when puzzleIndex changes: add `useEffect` that sets `setDisplayFen(puzzle.fen)` on `puzzleIndex` change
    d. In the correct-tap branch of `handleSquareClick` (line ~70-89):
       - `isAdvancing(true)` stays at its current position (line 76)
       - After setting isAdvancing, compute: `const newFen = moveFenPiece(puzzle.fen, puzzle.correctPieceSquare, puzzle.targetSquare)`
       - Call `setDisplayFen(newFen)` — the capturing piece slides to the target square, replacing the captured piece visually
       - All other behavior (green flash, confetti, celebration sound, 1500ms timeout) stays the same
    e. Change the Chessboard `position` prop from `puzzle.fen` to `displayFen`

    The animation effect: when a child taps the correct capturing piece, it slides across the board to the target square, visually "eating" the captured piece. react-chessboard handles the animation automatically since `animationDurationInMs: 200` is already configured.
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "moveFenPiece" app/\[locale\]/games/chess-game/CapturePuzzle.tsx (imported and used)
    - grep -q "displayFen" app/\[locale\]/games/chess-game/CapturePuzzle.tsx (new state drives board position)
    - grep -q "position.*displayFen" app/\[locale\]/games/chess-game/CapturePuzzle.tsx (Chessboard reads displayFen)
    - npm run build succeeds without errors
  </acceptance_criteria>
  <done>CapturePuzzle shows the capturing piece sliding to the target square on correct answer, replacing the captured piece visually.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run lint` passes
3. FEN helper unit tests pass
4. Manual verification: open chess game, play a movement puzzle, confirm piece slides on correct tap
5. Manual verification: play a capture puzzle, confirm capturing piece slides to target on correct tap
6. Confirm animation completes before confetti/celebration (simultaneous is fine per CONTEXT.md)
7. Confirm tapping is disabled during animation (isAdvancing blocks it)
</verification>

<success_criteria>
- Both puzzle types animate the piece moving on correct answer using react-chessboard's built-in animation
- Animation is triggered by updating a `displayFen` state that feeds the Chessboard `position` prop
- A reusable `moveFenPiece` utility exists in `utils/chessFen.ts` with tests
- No new dependencies added
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-puzzle-animations/09-01-SUMMARY.md`
</output>
