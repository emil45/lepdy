---
phase: 17-session-complete-progression-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/usePuzzleSession.ts
  - lib/featureFlags/types.ts
  - lib/featureFlags/providers/firebaseRemoteConfig.ts
  - messages/he.json
  - messages/en.json
  - messages/ru.json
autonomous: true
requirements: [SESS-03, DIFF-04]
must_haves:
  truths:
    - "usePuzzleSession exposes firstTryCount that increments only on correct first-tap answers"
    - "usePuzzleSession exposes sessionTiers and currentTiersByPiece forwarded from usePuzzleProgress"
    - "Star thresholds (3-star at 8, 2-star at 5) are configurable via Firebase Remote Config"
    - "Translation keys exist for mastery band names and session score in all 3 locales"
  artifacts:
    - path: "hooks/usePuzzleSession.ts"
      provides: "firstTryCount, sessionTiers, currentTiersByPiece in UsePuzzleSessionReturn"
      contains: "firstTryCount"
    - path: "lib/featureFlags/types.ts"
      provides: "chessStarThreshold3, chessStarThreshold2 flags"
      contains: "chessStarThreshold3"
    - path: "messages/he.json"
      provides: "Hebrew mastery band translations"
      contains: "masteryBeginner"
  key_links:
    - from: "hooks/usePuzzleSession.ts"
      to: "hooks/usePuzzleProgress.ts"
      via: "forwarding sessionTiers and data.pieces"
      pattern: "sessionTiers"
---

<objective>
Extend usePuzzleSession hook to expose first-try accuracy count and mastery tier data, add Firebase Remote Config flags for star thresholds, and add translation keys for the session complete screen.

Purpose: Provides all data and configuration the SessionCompleteScreen component (Plan 02) needs to render stars, mastery badges, and tier advancement feedback.
Output: Modified hook with 3 new return fields, 2 new feature flags, translation keys in 3 locales.
</objective>

<execution_context>
@/Users/emil/code/lepdy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emil/code/lepdy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-session-complete-progression-ui/17-CONTEXT.md
@.planning/phases/17-session-complete-progression-ui/17-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from usePuzzleProgress -->

From hooks/usePuzzleProgress.ts:
```typescript
export interface PiecePuzzleProgress {
  tier: 1 | 2 | 3;
  consecutiveCorrect: number;
  consecutiveWrong: number;
}

export interface UsePuzzleProgressReturn {
  getSessionTier: (pieceId: ChessPieceId) => 1 | 2 | 3;
  recordCorrect: (pieceId: ChessPieceId) => void;
  recordWrong: (pieceId: ChessPieceId) => void;
  sessionTiers: MutableRefObject<Record<string, 1 | 2 | 3>>;
  data: PuzzleProgressData;
}
```

From hooks/usePuzzleSession.ts:
```typescript
export interface UsePuzzleSessionReturn {
  currentPuzzle: SessionPuzzle | null;
  sessionIndex: number;
  consecutiveCorrect: number;
  isSessionComplete: boolean;
  onAnswer: (correct: boolean) => void;
  startNewSession: () => void;
}
```

From lib/featureFlags/types.ts:
```typescript
export interface FeatureFlags {
  showStickersButton: boolean;
  showVoiceIndicator: boolean;
  soundMatchingWrongAnswerDelayMs: number;
  chessAdvanceTierThreshold: number;
  chessDemoTierThreshold: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend usePuzzleSession with firstTryCount and forwarded tier data</name>
  <files>hooks/usePuzzleSession.ts</files>
  <read_first>hooks/usePuzzleSession.ts, hooks/usePuzzleProgress.ts</read_first>
  <action>
Modify hooks/usePuzzleSession.ts:

1. Import MutableRefObject from React and PiecePuzzleProgress from usePuzzleProgress.

2. Add three new fields to UsePuzzleSessionReturn interface:
   - firstTryCount: number
   - sessionTiers: MutableRefObject<Record<string, 1 | 2 | 3>>
   - currentTiersByPiece: Record<string, PiecePuzzleProgress>

3. In the hook body, destructure sessionTiers and data from usePuzzleProgress() alongside the existing getSessionTier, recordCorrect, recordWrong. The hook already calls usePuzzleProgress() at line 102 — just extend the destructuring.

4. Add state: const [firstTryCount, setFirstTryCount] = useState(0);

5. In the onAnswer callback, inside the `if (correct)` block (around line 185), add:
   setFirstTryCount((prev) => prev + 1);
   This goes BEFORE the existing setHeadIndex call. Because wrong answers do not advance headIndex (retry-in-place pattern from Phase 16), any onAnswer(true) is definitionally a first-try correct for that puzzle slot.

6. In the startNewSession callback, add: setFirstTryCount(0); alongside the existing setConsecutiveCorrect(0) reset.

7. In the return object, add the three new fields:
   firstTryCount,
   sessionTiers,
   currentTiersByPiece: data.pieces,

Do NOT persist firstTryCount to sessionStorage — it is session-memory-only per research decision. Add a code comment: "// firstTryCount is session-memory-only — mid-session refresh resets to 0 (acceptable for 10-puzzle session)"
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "firstTryCount" hooks/usePuzzleSession.ts
    - grep -q "sessionTiers" hooks/usePuzzleSession.ts
    - grep -q "currentTiersByPiece" hooks/usePuzzleSession.ts
    - grep -q "PiecePuzzleProgress" hooks/usePuzzleSession.ts
    - npm run lint exits 0
  </acceptance_criteria>
  <done>UsePuzzleSessionReturn has firstTryCount, sessionTiers, and currentTiersByPiece fields. firstTryCount increments on every correct answer. All three forwarded from internal usePuzzleProgress call without creating a second instance.</done>
</task>

<task type="auto">
  <name>Task 2: Add Firebase flags and translation keys for session complete screen</name>
  <files>lib/featureFlags/types.ts, lib/featureFlags/providers/firebaseRemoteConfig.ts, messages/he.json, messages/en.json, messages/ru.json</files>
  <read_first>lib/featureFlags/types.ts, lib/featureFlags/providers/firebaseRemoteConfig.ts, messages/he.json, messages/en.json, messages/ru.json</read_first>
  <action>
**Feature flags (lib/featureFlags/types.ts):**
Add two new numeric flags to FeatureFlags interface:
  chessStarThreshold3: number;  // First-try count for 3 stars (default 8)
  chessStarThreshold2: number;  // First-try count for 2 stars (default 5)

Add to DEFAULT_FLAGS:
  chessStarThreshold3: 8,
  chessStarThreshold2: 5,

**Firebase provider (lib/featureFlags/providers/firebaseRemoteConfig.ts):**
In fetchFlags() method, add to the newFlags object (around line 108):
  chessStarThreshold3: this.getNumberFlag('chessStarThreshold3', getValue),
  chessStarThreshold2: this.getNumberFlag('chessStarThreshold2', getValue),

**Translation keys — add under chessGame.ui in all 3 message files:**

Hebrew (messages/he.json) — add after the existing "newSession" key:
  "score": "{count}/10 נכון!",
  "masteryBeginner": "מתחיל",
  "masteryIntermediate": "בינוני",
  "masteryExpert": "מומחה",
  "gettingHarder": "!הולך ונהיה קשה",
  "startNewSession": "התחל מחדש"

English (messages/en.json) — add under chessGame.ui:
  "score": "{count}/10 correct!",
  "masteryBeginner": "Beginner",
  "masteryIntermediate": "Intermediate",
  "masteryExpert": "Expert",
  "gettingHarder": "Getting harder!",
  "startNewSession": "Start New Session"

Russian (messages/ru.json) — add under chessGame.ui:
  "score": "{count}/10 правильно!",
  "masteryBeginner": "Новичок",
  "masteryIntermediate": "Средний",
  "masteryExpert": "Эксперт",
  "gettingHarder": "Становится сложнее!",
  "startNewSession": "Начать заново"

Note: The existing "newSession" key in Hebrew says "התחל מחדש". The new "startNewSession" key is for the session complete screen button specifically. Keep both — "newSession" is used elsewhere.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "chessStarThreshold3" lib/featureFlags/types.ts
    - grep -q "chessStarThreshold2" lib/featureFlags/types.ts
    - grep -q "chessStarThreshold3" lib/featureFlags/providers/firebaseRemoteConfig.ts
    - grep -q "masteryBeginner" messages/he.json
    - grep -q "masteryBeginner" messages/en.json
    - grep -q "masteryBeginner" messages/ru.json
    - grep -q "gettingHarder" messages/he.json
    - npm run lint exits 0
  </acceptance_criteria>
  <done>Two new Firebase Remote Config flags with defaults (8 and 5). Translation keys for score, mastery bands (Beginner/Intermediate/Expert), and "Getting harder!" in all 3 locales.</done>
</task>

</tasks>

<verification>
- npm run lint passes with no errors
- UsePuzzleSessionReturn interface has 9 fields (6 existing + 3 new)
- FeatureFlags interface has 7 fields (5 existing + 2 new)
- All 3 message files have chessGame.ui.masteryBeginner, masteryIntermediate, masteryExpert, gettingHarder, score keys
</verification>

<success_criteria>
- usePuzzleSession hook exposes firstTryCount, sessionTiers, currentTiersByPiece
- Firebase Remote Config flags chessStarThreshold3 (default 8) and chessStarThreshold2 (default 5) exist
- Translation keys for mastery bands and session score exist in he/en/ru
- No lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-session-complete-progression-ui/17-01-SUMMARY.md`
</output>
