---
phase: 07-bug-fixes-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/chessPieces.ts
  - messages/he.json
  - messages/en.json
  - messages/ru.json
  - components/chess/ChessBoard.tsx
  - components/chess/ChessBoardDynamic.tsx
  - components/chess/useChessGame.ts
autonomous: true
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - "All chess piece names display translated text (not raw key paths) in all 3 locales"
    - "No orphaned Phase 2 chess files exist in the codebase"
    - "No unused chessGame.ui.* translation keys remain in message files"
    - "The app builds and all E2E tests pass"
  artifacts:
    - path: "data/chessPieces.ts"
      provides: "Chess piece configs with corrected translationKey values"
      contains: "pieces.king"
    - path: "messages/he.json"
      provides: "Hebrew translations without unused chessGame.ui keys"
    - path: "messages/en.json"
      provides: "English translations without unused chessGame.ui keys"
    - path: "messages/ru.json"
      provides: "Russian translations without unused chessGame.ui keys"
  key_links:
    - from: "data/chessPieces.ts"
      to: "PieceIntroduction.tsx, MovementPuzzle.tsx, CapturePuzzle.tsx"
      via: "t(config.translationKey) calls"
      pattern: "translationKey: 'pieces\\."
---

<objective>
Fix chess game translation key double-namespace bug and remove orphaned Phase 2 files and unused translation keys.

Purpose: Chess piece names currently display raw key paths (e.g., "chessGame.chessPieces.king") instead of translated text because `chessPieces.ts` includes the `chessGame.` prefix while components already scope via `useTranslations('chessGame')`. Orphaned files from Phase 2 add dead code to the repo.

Output: Working chess piece name display in all 3 locales, clean codebase with no dead code.
</objective>

<execution_context>
@/Users/emil/code/lepdy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emil/code/lepdy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bug-fixes-cleanup/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix translation key double-namespace in chessPieces.ts</name>
  <files>data/chessPieces.ts</files>
  <read_first>
    - data/chessPieces.ts (current broken translationKey values)
    - app/[locale]/games/chess-game/PieceIntroduction.tsx (consumer — uses t(config.translationKey))
    - app/[locale]/games/chess-game/MovementPuzzle.tsx (consumer — uses t(pieceConfig.translationKey))
    - app/[locale]/games/chess-game/CapturePuzzle.tsx (consumer — uses t(targetPieceConfig.translationKey))
    - messages/he.json (translation structure: chessGame.pieces.king etc.)
  </read_first>
  <action>
In data/chessPieces.ts, change the translationKey value for each piece from the double-namespaced form to the correct relative form. Components call useTranslations('chessGame') which scopes to the chessGame namespace, so keys must be relative to that namespace.

Change these 6 translationKey values:
- 'chessGame.pieces.king' -> 'pieces.king'
- 'chessGame.pieces.rook' -> 'pieces.rook'
- 'chessGame.pieces.bishop' -> 'pieces.bishop'
- 'chessGame.pieces.queen' -> 'pieces.queen'
- 'chessGame.pieces.knight' -> 'pieces.knight'
- 'chessGame.pieces.pawn' -> 'pieces.pawn'

No other fields in the file should be changed.
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && grep "translationKey:" data/chessPieces.ts | grep -c "chessGame\." | grep -q "^0$" && echo "PASS: no chessGame. prefix" || echo "FAIL: chessGame. prefix still present"</automated>
  </verify>
  <acceptance_criteria>
    - data/chessPieces.ts contains translationKey: 'pieces.king' (not 'chessGame.pieces.king')
    - data/chessPieces.ts contains translationKey: 'pieces.queen' (not 'chessGame.pieces.queen')
    - data/chessPieces.ts contains translationKey: 'pieces.rook' (not 'chessGame.pieces.rook')
    - data/chessPieces.ts contains translationKey: 'pieces.bishop' (not 'chessGame.pieces.bishop')
    - data/chessPieces.ts contains translationKey: 'pieces.knight' (not 'chessGame.pieces.knight')
    - data/chessPieces.ts contains translationKey: 'pieces.pawn' (not 'chessGame.pieces.pawn')
    - grep -c "chessGame\." data/chessPieces.ts returns 0
  </acceptance_criteria>
  <done>All 6 chess piece translationKey values use relative namespace (pieces.X) — components calling t(config.translationKey) with useTranslations('chessGame') will resolve correctly</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned files and unused translation keys</name>
  <files>
    components/chess/ChessBoard.tsx
    components/chess/ChessBoardDynamic.tsx
    components/chess/useChessGame.ts
    messages/he.json
    messages/en.json
    messages/ru.json
  </files>
  <read_first>
    - components/chess/ChessBoard.tsx (orphaned file to delete)
    - components/chess/ChessBoardDynamic.tsx (orphaned file to delete)
    - components/chess/useChessGame.ts (orphaned file to delete)
    - messages/he.json (contains unused chessGame.ui keys)
    - messages/en.json (contains unused chessGame.ui keys)
    - messages/ru.json (contains unused chessGame.ui keys)
  </read_first>
  <action>
1. Delete these 3 orphaned files (not imported by any component since Phases 5-6 use react-chessboard directly):
   - components/chess/ChessBoard.tsx
   - components/chess/ChessBoardDynamic.tsx
   - components/chess/useChessGame.ts

2. In each of the 3 translation files (messages/he.json, messages/en.json, messages/ru.json), remove these 5 unused keys from the chessGame.ui object:
   - "correct" (not referenced in any .ts/.tsx file under chessGame.ui scope)
   - "hint" (not referenced)
   - "tapToHear" (not referenced)
   - "findSquare" (not referenced)
   - "whichCaptures" (not referenced)

   Keep these 7 keys that ARE used by PieceIntroduction.tsx, MovementPuzzle.tsx, and CapturePuzzle.tsx:
   - "tryAgain", "next", "back", "levelComplete", "tapToMove", "tapToCapture", "learnedChess"

3. After edits, run `npm run lint` and `npm run build` to confirm no breakage.
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && test ! -f components/chess/ChessBoard.tsx && test ! -f components/chess/ChessBoardDynamic.tsx && test ! -f components/chess/useChessGame.ts && echo "PASS: orphaned files deleted" || echo "FAIL: orphaned files still exist"</automated>
  </verify>
  <acceptance_criteria>
    - components/chess/ChessBoard.tsx does not exist
    - components/chess/ChessBoardDynamic.tsx does not exist
    - components/chess/useChessGame.ts does not exist
    - grep "correct" messages/he.json does NOT match inside chessGame.ui block (key removed)
    - grep "hint" messages/he.json does NOT match inside chessGame.ui block
    - grep "tapToHear" messages/he.json returns no match inside chessGame section
    - grep "findSquare" messages/he.json returns no match
    - grep "whichCaptures" messages/he.json returns no match
    - messages/he.json still contains "tryAgain", "next", "back", "levelComplete", "tapToMove", "tapToCapture", "learnedChess" under chessGame.ui
    - Same removals verified in messages/en.json and messages/ru.json
    - npm run build exits 0
    - npm run lint exits 0
  </acceptance_criteria>
  <done>3 orphaned files deleted, 5 unused translation keys removed from all 3 locale files, build and lint pass</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run lint` passes
3. `npm test` E2E tests pass (chess game page loads correctly)
4. No file in codebase imports ChessBoard, ChessBoardDynamic, or useChessGame
5. Translation keys resolve correctly (no raw key paths displayed)
</verification>

<success_criteria>
- Chess piece names display translated text in all 3 locales (not "chessGame.pieces.king")
- Zero references to ChessBoard.tsx, ChessBoardDynamic.tsx, or useChessGame.ts in codebase
- Zero unused chessGame.ui.* keys in translation files
- Build, lint, and tests all pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-bug-fixes-cleanup/07-01-SUMMARY.md`
</output>
