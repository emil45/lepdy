---
phase: 08-navigation-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[locale]/games/chess-game/PieceIntroduction.tsx
  - app/[locale]/games/chess-game/MovementPuzzle.tsx
  - app/[locale]/games/chess-game/CapturePuzzle.tsx
autonomous: true
requirements: [NAV-01, NAV-02, UI-03]

must_haves:
  truths:
    - "A child can exit any puzzle screen back to the level map without using the browser back button"
    - "A child can exit the piece introduction walkthrough at any point and return to the level map"
    - "Next/Back arrows in piece introduction point correctly for the locale direction (left=Next in Hebrew RTL, right=Next in LTR)"
    - "The chess game main page back button already uses BackButton component (already satisfied, no change needed)"
  artifacts:
    - path: "app/[locale]/games/chess-game/PieceIntroduction.tsx"
      provides: "X exit button and RTL-aware arrow icons"
      contains: "CloseIcon"
    - path: "app/[locale]/games/chess-game/MovementPuzzle.tsx"
      provides: "X exit button for puzzle exit"
      contains: "CloseIcon"
    - path: "app/[locale]/games/chess-game/CapturePuzzle.tsx"
      provides: "X exit button for puzzle exit"
      contains: "CloseIcon"
  key_links:
    - from: "PieceIntroduction.tsx"
      to: "onComplete callback"
      via: "X button onClick calls onComplete()"
      pattern: "onClick.*onComplete"
    - from: "MovementPuzzle.tsx"
      to: "onComplete callback"
      via: "X button onClick calls onComplete()"
      pattern: "onClick.*onComplete"
    - from: "PieceIntroduction.tsx"
      to: "useDirection hook"
      via: "direction check for arrow icon swap"
      pattern: "useDirection"
---

<objective>
Add consistent exit navigation to all chess game sub-screens and fix RTL arrow direction in piece introduction.

Purpose: Children must be able to exit puzzles and the piece introduction walkthrough back to the level map without relying on the browser back button. Arrow directions must match locale reading direction.
Output: All three sub-views (PieceIntroduction, MovementPuzzle, CapturePuzzle) have an X exit button; PieceIntroduction arrows respect RTL/LTR.
</objective>

<execution_context>
@/Users/emil/code/lepdy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emil/code/lepdy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/[locale]/games/chess-game/ChessGameContent.tsx
@app/[locale]/games/chess-game/PieceIntroduction.tsx
@app/[locale]/games/chess-game/MovementPuzzle.tsx
@app/[locale]/games/chess-game/CapturePuzzle.tsx
@hooks/useDirection.ts
@components/BackButton.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From hooks/useDirection.ts:
```typescript
export function useDirection(): 'ltr' | 'rtl';
```

From ChessGameContent.tsx — onComplete callback pattern:
```typescript
// Each sub-view receives onComplete to return to map
<PieceIntroduction onComplete={() => setCurrentView('map')} completeLevel={completeLevel} />
<MovementPuzzle onComplete={() => setCurrentView('map')} completeLevel={completeLevel} />
<CapturePuzzle onComplete={() => setCurrentView('map')} completeLevel={completeLevel} />
```

From theme/theme.ts — no theme values needed for this plan (navigation only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add X exit button to PieceIntroduction and fix RTL arrow direction</name>
  <files>app/[locale]/games/chess-game/PieceIntroduction.tsx</files>
  <read_first>
    - app/[locale]/games/chess-game/PieceIntroduction.tsx
    - hooks/useDirection.ts
    - app/[locale]/games/chess-game/ChessGameContent.tsx
  </read_first>
  <action>
    Modify PieceIntroduction.tsx with two changes:

    **1. Add X exit button (NAV-01):**
    - Import `CloseIcon` from `@mui/icons-material/Close` and `IconButton` is already imported
    - Add an IconButton with CloseIcon at the top of the main return JSX (not the completion screen — per decision, no exit during celebration)
    - Position it absolutely in the top-right corner (or top-left in RTL — use `position: 'absolute', top: 8, right: 8` which MUI auto-flips in RTL)
    - Wait — the parent Box is not position:relative. Instead, add the X button as the first child inside the outer Box, using a wrapper Box with `sx={{ width: '100%', maxWidth: 400, display: 'flex', justifyContent: 'flex-end' }}`
    - The IconButton onClick calls `onComplete()` directly (reuses existing callback, per decision: no confirmation dialog)
    - Style: `sx={{ color: 'text.secondary' }}` with `aria-label="exit"`
    - data-testid="exit-button"

    **2. Fix RTL arrow direction (NAV-02):**
    - Import `useDirection` from `@/hooks/useDirection`
    - Call `const direction = useDirection();` inside the component
    - For the "Next" button: use `endIcon` when LTR, `startIcon` when RTL. The icon itself swaps:
      - LTR: `endIcon={<ArrowForwardIcon />}` (right arrow, advances forward)
      - RTL: `startIcon={<ArrowBackIcon />}` (left arrow in RTL = advances forward)
    - For the "Back" button: use `startIcon` when LTR, `endIcon` when RTL. The icon swaps:
      - LTR: `startIcon={<ArrowBackIcon />}` (left arrow, goes back)
      - RTL: `endIcon={<ArrowForwardIcon />}` (right arrow in RTL = goes back)
    - Implementation: define the icon props conditionally:
      ```
      const nextButtonProps = direction === 'rtl'
        ? { startIcon: <ArrowBackIcon /> }
        : { endIcon: <ArrowForwardIcon /> };
      const backButtonProps = direction === 'rtl'
        ? { endIcon: <ArrowForwardIcon /> }
        : { startIcon: <ArrowBackIcon /> };
      ```
    - Spread these onto the respective Button components
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CloseIcon" app/[locale]/games/chess-game/PieceIntroduction.tsx
    - grep -q "exit-button" app/[locale]/games/chess-game/PieceIntroduction.tsx
    - grep -q "useDirection" app/[locale]/games/chess-game/PieceIntroduction.tsx
    - grep -q "direction === 'rtl'" app/[locale]/games/chess-game/PieceIntroduction.tsx
    - grep -q "onComplete" app/[locale]/games/chess-game/PieceIntroduction.tsx (X button calls onComplete)
  </acceptance_criteria>
  <done>PieceIntroduction has an X exit button that returns to level map, and Next/Back arrows point in the correct direction for RTL and LTR locales</done>
</task>

<task type="auto">
  <name>Task 2: Add X exit button to MovementPuzzle and CapturePuzzle</name>
  <files>app/[locale]/games/chess-game/MovementPuzzle.tsx, app/[locale]/games/chess-game/CapturePuzzle.tsx</files>
  <read_first>
    - app/[locale]/games/chess-game/MovementPuzzle.tsx
    - app/[locale]/games/chess-game/CapturePuzzle.tsx
  </read_first>
  <action>
    Apply the same X exit button pattern to both puzzle components (NAV-01):

    **For MovementPuzzle.tsx:**
    - Import `CloseIcon` from `@mui/icons-material/Close` and `IconButton` from `@mui/material/IconButton`
    - Add exit button as first child inside the main return Box (not the completion screen)
    - Use a wrapper: `<Box sx={{ width: '100%', maxWidth: 480, display: 'flex', justifyContent: 'flex-end' }}>`
    - Inside: `<IconButton onClick={onComplete} sx={{ color: 'text.secondary' }} aria-label="exit" data-testid="exit-button"><CloseIcon /></IconButton>`

    **For CapturePuzzle.tsx:**
    - Identical pattern — import CloseIcon and IconButton
    - Add the same exit button wrapper as first child inside the main return Box (not the completion screen)
    - Same sx, aria-label, data-testid, onClick={onComplete}

    Both components already have `onComplete` in their props — the X button simply calls it directly. Per user decision: no confirmation dialog needed.
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CloseIcon" app/[locale]/games/chess-game/MovementPuzzle.tsx
    - grep -q "exit-button" app/[locale]/games/chess-game/MovementPuzzle.tsx
    - grep -q "CloseIcon" app/[locale]/games/chess-game/CapturePuzzle.tsx
    - grep -q "exit-button" app/[locale]/games/chess-game/CapturePuzzle.tsx
  </acceptance_criteria>
  <done>Both puzzle components have an X exit button in the top-right corner that returns to the level map via onComplete()</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- All 3 sub-views (PieceIntroduction, MovementPuzzle, CapturePuzzle) have `data-testid="exit-button"` elements
- PieceIntroduction imports and uses `useDirection` for arrow direction logic
- E2E tests still pass: `npm test`
</verification>

<success_criteria>
- Every chess game sub-screen has an X exit button visible during gameplay (not during celebration)
- PieceIntroduction Next arrow points left in Hebrew, right in English/Russian
- PieceIntroduction Back arrow points right in Hebrew, left in English/Russian
- The main chess page BackButton is unchanged (already correct per UI-03)
</success_criteria>

<output>
After completion, create `.planning/phases/08-navigation-ui-polish/08-01-SUMMARY.md`
</output>
