---
phase: 08-navigation-ui-polish
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - app/[locale]/games/chess-game/ChessGameContent.tsx
  - app/[locale]/games/chess-game/PieceIntroduction.tsx
  - app/[locale]/games/chess-game/MovementPuzzle.tsx
  - app/[locale]/games/chess-game/CapturePuzzle.tsx
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "The chess game uses Lepdy's pastel color palette with rounded cards and soft shadows"
    - "Screens transition with a smooth fade instead of an instant swap when switching between map and level views"
    - "The chess board area is wrapped in a soft-shadow card with rounded corners"
    - "Puzzle instruction areas have rounded corners and soft shadows"
  artifacts:
    - path: "app/[locale]/games/chess-game/ChessGameContent.tsx"
      provides: "Fade transitions between map and level views, soft shadows on level cards"
      contains: "Fade"
    - path: "app/[locale]/games/chess-game/PieceIntroduction.tsx"
      provides: "Soft shadow on piece card"
      contains: "boxShadow"
    - path: "app/[locale]/games/chess-game/MovementPuzzle.tsx"
      provides: "Soft shadow card around board and instruction area"
      contains: "boxShadow"
    - path: "app/[locale]/games/chess-game/CapturePuzzle.tsx"
      provides: "Soft shadow card around board and instruction area"
      contains: "boxShadow"
  key_links:
    - from: "ChessGameContent.tsx"
      to: "MUI Fade component"
      via: "Fade wrapping view rendering with key-based transitions"
      pattern: "<Fade"
    - from: "LevelMapCard"
      to: "theme pastels"
      via: "soft shadow styling on cards"
      pattern: "boxShadow"
---

<objective>
Apply Lepdy's pastel visual style with soft shadows and rounded corners to all chess game components, and add fade transitions between map and level views.

Purpose: The chess game should visually match Lepdy's playful, child-friendly aesthetic with smooth view changes instead of jarring instant swaps.
Output: Chess game components use pastel styling with soft shadows; view transitions animate with a 300ms fade.
</objective>

<execution_context>
@/Users/emil/code/lepdy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emil/code/lepdy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-navigation-ui-polish/08-01-SUMMARY.md

@app/[locale]/games/chess-game/ChessGameContent.tsx
@app/[locale]/games/chess-game/PieceIntroduction.tsx
@app/[locale]/games/chess-game/MovementPuzzle.tsx
@app/[locale]/games/chess-game/CapturePuzzle.tsx
@theme/theme.ts

<interfaces>
<!-- Theme pastel colors from theme/theme.ts -->
```typescript
palette.colors: {
  redPastel: '#eeaea8',
  purplePastel: '#dbc3e2',
  beigePastel: '#f5ede1',
  greenPastel: '#dee581',
  bluePastel: '#9ed6ea',
  orangePastel: '#ffcd36',
  blackPastel: '#434243',
}
```

<!-- Soft shadow value from CONTEXT.md decisions -->
boxShadow: '0 2px 8px rgba(0,0,0,0.1)'
borderRadius: 3-4

<!-- MUI Fade component -->
```typescript
import Fade from '@mui/material/Fade';
// Usage: <Fade in={true} timeout={300}><div>content</div></Fade>
// Fade requires a single child that accepts ref (DOM element or forwardRef component)
```

<!-- Level cards already use these colors -->
LEVELS = [
  { num: 1, color: '#9ed6ea' },  // bluePastel
  { num: 2, color: '#dbc3e2' },  // purplePastel
  { num: 3, color: '#ffcd36' },  // orangePastel
];
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fade transitions and soft shadows to ChessGameContent level map</name>
  <files>app/[locale]/games/chess-game/ChessGameContent.tsx</files>
  <read_first>
    - app/[locale]/games/chess-game/ChessGameContent.tsx
    - theme/theme.ts
  </read_first>
  <action>
    Modify ChessGameContent.tsx with two changes:

    **1. Fade transitions between views (UI-02):**
    - Import `Fade` from `@mui/material/Fade`
    - Wrap each view return in a `<Fade in={true} timeout={300}>` with a `<div>` child (Fade needs a DOM element child that accepts ref)
    - The map view (the main return with level cards) gets wrapped: `<Fade in={true} timeout={300}><div>...existing map JSX...</div></Fade>`
    - Each level view return gets wrapped the same way:
      ```tsx
      if (currentView === 'level-1') {
        return <Fade in={true} timeout={300}><div><PieceIntroduction ... /></div></Fade>;
      }
      ```
    - Same for level-2 and level-3 returns
    - Per decision: 300ms duration, only full view swaps animate (not within-level puzzle transitions)

    **2. Soft shadows on LevelMapCard (UI-01):**
    - Add `boxShadow: '0 2px 8px rgba(0,0,0,0.1)'` to the LevelMapCard Card component's sx prop
    - The Card already has `borderRadius: 3` which is correct
    - Keep the existing pastel bgColor values — they already match the Lepdy palette (bluePastel, purplePastel, orangePastel)
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Fade" app/[locale]/games/chess-game/ChessGameContent.tsx
    - grep -q "timeout={300}" app/[locale]/games/chess-game/ChessGameContent.tsx
    - grep -q "boxShadow" app/[locale]/games/chess-game/ChessGameContent.tsx
    - grep -q "0 2px 8px rgba(0,0,0,0.1)" app/[locale]/games/chess-game/ChessGameContent.tsx
  </acceptance_criteria>
  <done>Level map cards have soft shadows and all view transitions use a 300ms fade animation</done>
</task>

<task type="auto">
  <name>Task 2: Add pastel styling with soft shadows to puzzle and intro components</name>
  <files>app/[locale]/games/chess-game/PieceIntroduction.tsx, app/[locale]/games/chess-game/MovementPuzzle.tsx, app/[locale]/games/chess-game/CapturePuzzle.tsx</files>
  <read_first>
    - app/[locale]/games/chess-game/PieceIntroduction.tsx
    - app/[locale]/games/chess-game/MovementPuzzle.tsx
    - app/[locale]/games/chess-game/CapturePuzzle.tsx
  </read_first>
  <action>
    Apply consistent soft-shadow card styling to all three sub-view components (UI-01):

    **PieceIntroduction.tsx:**
    - The piece card Box already has `borderRadius: 4` and `bgcolor: currentPiece.color` (pastel colors from chessPieces data) — this is good
    - Add `boxShadow: '0 2px 8px rgba(0,0,0,0.1)'` to the piece card Box sx prop (the Box with `bgcolor: currentPiece.color`)
    - No other changes needed — the piece card is already styled with rounded corners and pastel colors

    **MovementPuzzle.tsx:**
    - Wrap the board container (the Box with `ref={containerRef}`) and the instruction text above it in a parent Card-style Box:
      ```tsx
      <Box sx={{
        bgcolor: '#f5ede1',  // beigePastel
        borderRadius: 3,
        boxShadow: '0 2px 8px rgba(0,0,0,0.1)',
        p: 2,
        width: '100%',
        maxWidth: 480,
      }}>
        {/* instruction text Typography moves inside here */}
        {/* board container Box stays inside here */}
      </Box>
      ```
    - Move the instruction Typography (`t('ui.tapToMove', ...)`) inside this wrapper Box
    - The board container Box keeps its existing `ref={containerRef}` and `direction: 'ltr'` props

    **CapturePuzzle.tsx:**
    - Apply the identical wrapper pattern as MovementPuzzle:
      ```tsx
      <Box sx={{
        bgcolor: '#f5ede1',  // beigePastel
        borderRadius: 3,
        boxShadow: '0 2px 8px rgba(0,0,0,0.1)',
        p: 2,
        width: '100%',
        maxWidth: 480,
      }}>
        {/* instruction text Typography moves inside here */}
        {/* board container Box stays inside here */}
      </Box>
      ```
    - Move the instruction Typography (`t('ui.tapToCapture', ...)`) inside this wrapper
  </action>
  <verify>
    <automated>cd /Users/emil/code/lepdy && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "boxShadow" app/[locale]/games/chess-game/PieceIntroduction.tsx
    - grep -q "0 2px 8px rgba(0,0,0,0.1)" app/[locale]/games/chess-game/PieceIntroduction.tsx
    - grep -q "boxShadow" app/[locale]/games/chess-game/MovementPuzzle.tsx
    - grep -q "f5ede1" app/[locale]/games/chess-game/MovementPuzzle.tsx
    - grep -q "borderRadius: 3" app/[locale]/games/chess-game/MovementPuzzle.tsx
    - grep -q "boxShadow" app/[locale]/games/chess-game/CapturePuzzle.tsx
    - grep -q "f5ede1" app/[locale]/games/chess-game/CapturePuzzle.tsx
    - grep -q "borderRadius: 3" app/[locale]/games/chess-game/CapturePuzzle.tsx
  </acceptance_criteria>
  <done>All chess game components use Lepdy's pastel palette with rounded corners and soft shadows on card-style containers around boards and piece cards</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- All modified files contain `boxShadow` styling
- ChessGameContent.tsx contains `Fade` import and `timeout={300}`
- Puzzle components use beigePastel (`#f5ede1`) background
- E2E tests still pass: `npm test`
</verification>

<success_criteria>
- Level map cards have soft shadows matching Lepdy's visual style
- View transitions between map and levels use a 300ms fade animation
- Chess board areas in both puzzle types are wrapped in a soft-shadow card with rounded corners and beigePastel background
- Piece introduction card has a soft shadow added to its existing pastel styling
- No visual regressions in existing game functionality
</success_criteria>

<output>
After completion, create `.planning/phases/08-navigation-ui-polish/08-02-SUMMARY.md`
</output>
